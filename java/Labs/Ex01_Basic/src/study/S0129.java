package study;

public class S0129 {

	public static void main(String[] args) {
		
		// class = 설계도 == 데이터 타입(Data Type)
		//        (new) -> 메모리에 올릴 때 사용하는 메서드
		// 객체 = class(설계도) 를 가지고 구체화 된 것.(메모리에 오른 것)
		// class가 객체화 되는 과정 : instance화 (인스턴스화)
		
		// 속성 : 고유 속성, 상태 속성, 참조 속성
	}

}
